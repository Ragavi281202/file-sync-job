from setuptools import setup, find_packages

setup(
    name="file-sync-job",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here if needed
        "requests"
    ],
)
